{
  "name": "@streamerson/examples",
  "version": "0.0.1",
  "scripts": {
    "start:crud-app": "docker compose up -d && bun --watch ./crud-app/api | pino-pretty",
    "build": "tsc -b tsconfig.lib.json",
    "test": "bun test"
  },
  "dependencies": {
    "@streamerson/consumer": "workspace:*",
    "@streamerson/gateway-fastify": "workspace:*",
    "@streamerson/gateway-wss": "workspace:*",
    "fastify": "^5.11.0",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2"
  },
  "type": "commonjs"
}
