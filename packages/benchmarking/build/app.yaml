services:
  benchmarking:
    image: ${STREAMERSON_IMAGE_TARGET}
    depends_on:
      - redis
    networks:
      - streamerson
    environment:
      IMAGE: ${STREAMERSON_IMAGE_TARGET}
      STREAMERSON_BENCHMARK_APP_TARGET: ${STREAMERSON_BENCHMARK_APP_TARGET}
      STREAMERSON_REDIS_HOST: "streamerson"

