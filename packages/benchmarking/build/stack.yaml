services:
  redis:
    container_name: "streamerson-redis-${STREAMERSON_PROJECT}"
    hostname: streamerson
    image: "redis:alpine"
    ports:
      - 0.0.0.0:6379:6379
    networks:
      - streamerson
networks:
  streamerson:
    name: streamerson
